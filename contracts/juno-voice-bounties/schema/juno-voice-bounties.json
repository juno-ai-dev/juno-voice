{
  "contract_name": "juno-voice-bounties",
  "contract_version": "1.0.0",
  "idl_version": "1.0.0",
  "instantiate": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "InstantiateMsg",
    "type": "object",
    "required": [
      "agent",
      "governor",
      "limits",
      "max_bounty_total",
      "max_contributors",
      "max_lifetime_seconds",
      "max_rounds",
      "min_contribution",
      "min_lifetime_seconds",
      "native_denom",
      "registry"
    ],
    "properties": {
      "agent": {
        "type": "string"
      },
      "governor": {
        "type": "string"
      },
      "limits": {
        "$ref": "#/definitions/Limits"
      },
      "max_bounty_total": {
        "$ref": "#/definitions/Uint128"
      },
      "max_contributors": {
        "type": "integer",
        "format": "uint32",
        "minimum": 0.0
      },
      "max_lifetime_seconds": {
        "type": "integer",
        "format": "uint64",
        "minimum": 0.0
      },
      "max_rounds": {
        "type": "integer",
        "format": "uint32",
        "minimum": 0.0
      },
      "min_contribution": {
        "$ref": "#/definitions/Uint128"
      },
      "min_lifetime_seconds": {
        "type": "integer",
        "format": "uint64",
        "minimum": 0.0
      },
      "native_denom": {
        "type": "string"
      },
      "registry": {
        "type": "string"
      }
    },
    "additionalProperties": false,
    "definitions": {
      "Limits": {
        "type": "object",
        "required": [
          "max_acceptance_criteria_bytes",
          "max_page_limit",
          "max_rationale_bytes",
          "max_reason_bytes",
          "max_summary_bytes",
          "max_title_bytes",
          "max_uri_bytes"
        ],
        "properties": {
          "max_acceptance_criteria_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_page_limit": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_rationale_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_reason_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_summary_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_title_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_uri_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          }
        },
        "additionalProperties": false
      },
      "Uint128": {
        "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
        "type": "string"
      }
    }
  },
  "execute": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ExecuteMsg",
    "oneOf": [
      {
        "type": "object",
        "required": [
          "create_bounty"
        ],
        "properties": {
          "create_bounty": {
            "type": "object",
            "required": [
              "acceptance_criteria",
              "expires_at",
              "summary",
              "title"
            ],
            "properties": {
              "acceptance_criteria": {
                "type": "string"
              },
              "content_digest": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "content_uri": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "expires_at": {
                "$ref": "#/definitions/Timestamp"
              },
              "project_candidate": {
                "anyOf": [
                  {
                    "$ref": "#/definitions/ProjectCandidate"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "summary": {
                "type": "string"
              },
              "title": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "contribute"
        ],
        "properties": {
          "contribute": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "nominate_payout"
        ],
        "properties": {
          "nominate_payout": {
            "type": "object",
            "required": [
              "bounty_id",
              "evidence_digest",
              "evidence_uri",
              "rationale",
              "recipient"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "evidence_digest": {
                "type": "string"
              },
              "evidence_uri": {
                "type": "string"
              },
              "rationale": {
                "type": "string"
              },
              "recipient": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "confirm_sole_payout"
        ],
        "properties": {
          "confirm_sole_payout": {
            "type": "object",
            "required": [
              "bounty_id",
              "round"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "round": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "decline_sole_payout"
        ],
        "properties": {
          "decline_sole_payout": {
            "type": "object",
            "required": [
              "bounty_id",
              "reason",
              "round"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "reason": {
                "type": "string"
              },
              "round": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "vote_payout"
        ],
        "properties": {
          "vote_payout": {
            "type": "object",
            "required": [
              "bounty_id",
              "round",
              "vote"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "rationale": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "round": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "vote": {
                "$ref": "#/definitions/PayoutVote"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "finalize_payout"
        ],
        "properties": {
          "finalize_payout": {
            "type": "object",
            "required": [
              "bounty_id",
              "round"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "round": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "cancel_sole_funded"
        ],
        "properties": {
          "cancel_sole_funded": {
            "type": "object",
            "required": [
              "bounty_id",
              "reason"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "reason": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "expire"
        ],
        "properties": {
          "expire": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "claim_refund"
        ],
        "properties": {
          "claim_refund": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "moderate"
        ],
        "properties": {
          "moderate": {
            "type": "object",
            "required": [
              "bounty_id",
              "outcome",
              "reason"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "outcome": {
                "$ref": "#/definitions/ModerationOutcome"
              },
              "reason": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "graduate_project"
        ],
        "properties": {
          "graduate_project": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "pause_new_activity"
        ],
        "properties": {
          "pause_new_activity": {
            "type": "object",
            "required": [
              "reason"
            ],
            "properties": {
              "reason": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "unpause_new_activity"
        ],
        "properties": {
          "unpause_new_activity": {
            "type": "object",
            "required": [
              "reason"
            ],
            "properties": {
              "reason": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "update_roles"
        ],
        "properties": {
          "update_roles": {
            "type": "object",
            "properties": {
              "agent": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "governor": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "registry": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "update_config"
        ],
        "properties": {
          "update_config": {
            "type": "object",
            "required": [
              "update"
            ],
            "properties": {
              "update": {
                "$ref": "#/definitions/ConfigUpdate"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ],
    "definitions": {
      "ConfigUpdate": {
        "type": "object",
        "properties": {
          "limits": {
            "anyOf": [
              {
                "$ref": "#/definitions/Limits"
              },
              {
                "type": "null"
              }
            ]
          },
          "max_bounty_total": {
            "anyOf": [
              {
                "$ref": "#/definitions/Uint128"
              },
              {
                "type": "null"
              }
            ]
          },
          "max_contributors": {
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "minimum": 0.0
          },
          "max_lifetime_seconds": {
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "minimum": 0.0
          },
          "max_rounds": {
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "minimum": 0.0
          },
          "min_contribution": {
            "anyOf": [
              {
                "$ref": "#/definitions/Uint128"
              },
              {
                "type": "null"
              }
            ]
          },
          "min_lifetime_seconds": {
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "minimum": 0.0
          }
        },
        "additionalProperties": false
      },
      "Limits": {
        "type": "object",
        "required": [
          "max_acceptance_criteria_bytes",
          "max_page_limit",
          "max_rationale_bytes",
          "max_reason_bytes",
          "max_summary_bytes",
          "max_title_bytes",
          "max_uri_bytes"
        ],
        "properties": {
          "max_acceptance_criteria_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_page_limit": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_rationale_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_reason_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_summary_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_title_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "max_uri_bytes": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          }
        },
        "additionalProperties": false
      },
      "ModerationOutcome": {
        "type": "string",
        "enum": [
          "spam",
          "duplicate",
          "policy_violation"
        ]
      },
      "PayoutVote": {
        "type": "string",
        "enum": [
          "yes",
          "no"
        ]
      },
      "ProjectCandidate": {
        "type": "object",
        "required": [
          "metadata_digest",
          "metadata_uri",
          "project_id"
        ],
        "properties": {
          "metadata_digest": {
            "type": "string"
          },
          "metadata_uri": {
            "type": "string"
          },
          "project_id": {
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "Timestamp": {
        "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
        "allOf": [
          {
            "$ref": "#/definitions/Uint64"
          }
        ]
      },
      "Uint128": {
        "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
        "type": "string"
      },
      "Uint64": {
        "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
        "type": "string"
      }
    }
  },
  "query": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "QueryMsg",
    "oneOf": [
      {
        "type": "object",
        "required": [
          "config"
        ],
        "properties": {
          "config": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "pause"
        ],
        "properties": {
          "pause": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "authorities"
        ],
        "properties": {
          "authorities": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "accounting"
        ],
        "properties": {
          "accounting": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "health"
        ],
        "properties": {
          "health": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "bounty"
        ],
        "properties": {
          "bounty": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "bounties"
        ],
        "properties": {
          "bounties": {
            "type": "object",
            "properties": {
              "limit": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "start_after": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "contribution"
        ],
        "properties": {
          "contribution": {
            "type": "object",
            "required": [
              "bounty_id",
              "contributor"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "contributor": {
                "type": "string"
              },
              "round": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "contributions"
        ],
        "properties": {
          "contributions": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "limit": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "start_after": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "round"
        ],
        "properties": {
          "round": {
            "type": "object",
            "required": [
              "bounty_id",
              "round"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "round": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "rounds"
        ],
        "properties": {
          "rounds": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "limit": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "start_after": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "receipt"
        ],
        "properties": {
          "receipt": {
            "type": "object",
            "required": [
              "bounty_id",
              "round",
              "voter"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "round": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "voter": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "receipts"
        ],
        "properties": {
          "receipts": {
            "type": "object",
            "required": [
              "bounty_id",
              "round"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "limit": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "round": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "start_after": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "claim"
        ],
        "properties": {
          "claim": {
            "type": "object",
            "required": [
              "bounty_id",
              "contributor"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "contributor": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "claims"
        ],
        "properties": {
          "claims": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "limit": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "start_after": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "history"
        ],
        "properties": {
          "history": {
            "type": "object",
            "required": [
              "bounty_id"
            ],
            "properties": {
              "bounty_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "limit": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "start_after": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "error_catalog"
        ],
        "properties": {
          "error_catalog": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ]
  },
  "migrate": null,
  "sudo": null,
  "responses": {
    "accounting": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Accounting",
      "type": "object",
      "required": [
        "active_escrow",
        "lifetime_paid",
        "lifetime_received",
        "lifetime_refunded",
        "outstanding_refunds",
        "pending_payout_liabilities"
      ],
      "properties": {
        "active_escrow": {
          "$ref": "#/definitions/Uint128"
        },
        "lifetime_paid": {
          "$ref": "#/definitions/Uint128"
        },
        "lifetime_received": {
          "$ref": "#/definitions/Uint128"
        },
        "lifetime_refunded": {
          "$ref": "#/definitions/Uint128"
        },
        "outstanding_refunds": {
          "$ref": "#/definitions/Uint128"
        },
        "pending_payout_liabilities": {
          "$ref": "#/definitions/Uint128"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "authorities": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AuthoritiesResponse",
      "type": "object",
      "required": [
        "agent",
        "governor",
        "registry"
      ],
      "properties": {
        "agent": {
          "$ref": "#/definitions/Addr"
        },
        "governor": {
          "$ref": "#/definitions/Addr"
        },
        "registry": {
          "$ref": "#/definitions/Addr"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        }
      }
    },
    "bounties": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BountiesResponse",
      "type": "object",
      "required": [
        "bounties"
      ],
      "properties": {
        "bounties": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Bounty"
          }
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Bounty": {
          "type": "object",
          "required": [
            "contributor_count",
            "created_at",
            "creator",
            "expires_at",
            "history_count",
            "id",
            "next_round",
            "paid_amount",
            "refunded_amount",
            "status",
            "terms",
            "total_contribution"
          ],
          "properties": {
            "active_round": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0.0
            },
            "contributor_count": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "created_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "creator": {
              "$ref": "#/definitions/Addr"
            },
            "expires_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "graduated_at": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Timestamp"
                },
                {
                  "type": "null"
                }
              ]
            },
            "history_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "next_round": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "paid_amount": {
              "$ref": "#/definitions/Uint128"
            },
            "paid_at": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Timestamp"
                },
                {
                  "type": "null"
                }
              ]
            },
            "paid_recipient": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Addr"
                },
                {
                  "type": "null"
                }
              ]
            },
            "project_candidate": {
              "anyOf": [
                {
                  "$ref": "#/definitions/ProjectCandidate"
                },
                {
                  "type": "null"
                }
              ]
            },
            "refund_reason": {
              "anyOf": [
                {
                  "$ref": "#/definitions/RefundReason"
                },
                {
                  "type": "null"
                }
              ]
            },
            "refunded_amount": {
              "$ref": "#/definitions/Uint128"
            },
            "status": {
              "$ref": "#/definitions/BountyStatus"
            },
            "terms": {
              "$ref": "#/definitions/Terms"
            },
            "total_contribution": {
              "$ref": "#/definitions/Uint128"
            }
          },
          "additionalProperties": false
        },
        "BountyStatus": {
          "type": "string",
          "enum": [
            "open",
            "single_confirmation",
            "ratifying",
            "refunding",
            "refunded",
            "paid"
          ]
        },
        "ModerationOutcome": {
          "type": "string",
          "enum": [
            "spam",
            "duplicate",
            "policy_violation"
          ]
        },
        "ProjectCandidate": {
          "type": "object",
          "required": [
            "metadata_digest",
            "metadata_uri",
            "project_id"
          ],
          "properties": {
            "metadata_digest": {
              "type": "string"
            },
            "metadata_uri": {
              "type": "string"
            },
            "project_id": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "RefundReason": {
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "expired",
                "sole_confirmation_timeout",
                "round_limit"
              ]
            },
            {
              "type": "object",
              "required": [
                "cancelled"
              ],
              "properties": {
                "cancelled": {
                  "type": "object",
                  "required": [
                    "reason"
                  ],
                  "properties": {
                    "reason": {
                      "type": "string"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": [
                "moderated"
              ],
              "properties": {
                "moderated": {
                  "type": "object",
                  "required": [
                    "outcome",
                    "reason"
                  ],
                  "properties": {
                    "outcome": {
                      "$ref": "#/definitions/ModerationOutcome"
                    },
                    "reason": {
                      "type": "string"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "Terms": {
          "type": "object",
          "required": [
            "acceptance_criteria",
            "config_version",
            "max_bounty_total",
            "max_contributors",
            "max_evidence_uri_bytes",
            "max_rationale_bytes",
            "max_reason_bytes",
            "max_rounds",
            "ratification_seconds",
            "summary",
            "title"
          ],
          "properties": {
            "acceptance_criteria": {
              "type": "string"
            },
            "config_version": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "content_digest": {
              "type": [
                "string",
                "null"
              ]
            },
            "content_uri": {
              "type": [
                "string",
                "null"
              ]
            },
            "max_bounty_total": {
              "$ref": "#/definitions/Uint128"
            },
            "max_contributors": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_evidence_uri_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_rationale_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_reason_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_rounds": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "ratification_seconds": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "summary": {
              "type": "string"
            },
            "title": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        }
      }
    },
    "bounty": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BountyResponse",
      "type": "object",
      "required": [
        "bounty"
      ],
      "properties": {
        "active_round": {
          "anyOf": [
            {
              "$ref": "#/definitions/Round"
            },
            {
              "type": "null"
            }
          ]
        },
        "bounty": {
          "$ref": "#/definitions/Bounty"
        },
        "graduation": {
          "anyOf": [
            {
              "$ref": "#/definitions/GraduationRecord"
            },
            {
              "type": "null"
            }
          ]
        },
        "moderation": {
          "anyOf": [
            {
              "$ref": "#/definitions/ModerationRecord"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Bounty": {
          "type": "object",
          "required": [
            "contributor_count",
            "created_at",
            "creator",
            "expires_at",
            "history_count",
            "id",
            "next_round",
            "paid_amount",
            "refunded_amount",
            "status",
            "terms",
            "total_contribution"
          ],
          "properties": {
            "active_round": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0.0
            },
            "contributor_count": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "created_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "creator": {
              "$ref": "#/definitions/Addr"
            },
            "expires_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "graduated_at": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Timestamp"
                },
                {
                  "type": "null"
                }
              ]
            },
            "history_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "next_round": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "paid_amount": {
              "$ref": "#/definitions/Uint128"
            },
            "paid_at": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Timestamp"
                },
                {
                  "type": "null"
                }
              ]
            },
            "paid_recipient": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Addr"
                },
                {
                  "type": "null"
                }
              ]
            },
            "project_candidate": {
              "anyOf": [
                {
                  "$ref": "#/definitions/ProjectCandidate"
                },
                {
                  "type": "null"
                }
              ]
            },
            "refund_reason": {
              "anyOf": [
                {
                  "$ref": "#/definitions/RefundReason"
                },
                {
                  "type": "null"
                }
              ]
            },
            "refunded_amount": {
              "$ref": "#/definitions/Uint128"
            },
            "status": {
              "$ref": "#/definitions/BountyStatus"
            },
            "terms": {
              "$ref": "#/definitions/Terms"
            },
            "total_contribution": {
              "$ref": "#/definitions/Uint128"
            }
          },
          "additionalProperties": false
        },
        "BountyStatus": {
          "type": "string",
          "enum": [
            "open",
            "single_confirmation",
            "ratifying",
            "refunding",
            "refunded",
            "paid"
          ]
        },
        "GraduationRecord": {
          "type": "object",
          "required": [
            "agent",
            "bounty_id",
            "graduated_at",
            "payout_address",
            "project_id",
            "registry"
          ],
          "properties": {
            "agent": {
              "$ref": "#/definitions/Addr"
            },
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "graduated_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "payout_address": {
              "$ref": "#/definitions/Addr"
            },
            "project_id": {
              "type": "string"
            },
            "registry": {
              "$ref": "#/definitions/Addr"
            }
          },
          "additionalProperties": false
        },
        "ModerationOutcome": {
          "type": "string",
          "enum": [
            "spam",
            "duplicate",
            "policy_violation"
          ]
        },
        "ModerationRecord": {
          "type": "object",
          "required": [
            "bounty_id",
            "moderated_at",
            "moderator",
            "outcome",
            "reason"
          ],
          "properties": {
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "moderated_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "moderator": {
              "$ref": "#/definitions/Addr"
            },
            "outcome": {
              "$ref": "#/definitions/ModerationOutcome"
            },
            "reason": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "Nomination": {
          "type": "object",
          "required": [
            "evidence_digest",
            "evidence_uri",
            "nominator",
            "rationale",
            "recipient"
          ],
          "properties": {
            "evidence_digest": {
              "type": "string"
            },
            "evidence_uri": {
              "type": "string"
            },
            "nominator": {
              "$ref": "#/definitions/Addr"
            },
            "rationale": {
              "type": "string"
            },
            "recipient": {
              "$ref": "#/definitions/Addr"
            }
          },
          "additionalProperties": false
        },
        "ProjectCandidate": {
          "type": "object",
          "required": [
            "metadata_digest",
            "metadata_uri",
            "project_id"
          ],
          "properties": {
            "metadata_digest": {
              "type": "string"
            },
            "metadata_uri": {
              "type": "string"
            },
            "project_id": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "RefundReason": {
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "expired",
                "sole_confirmation_timeout",
                "round_limit"
              ]
            },
            {
              "type": "object",
              "required": [
                "cancelled"
              ],
              "properties": {
                "cancelled": {
                  "type": "object",
                  "required": [
                    "reason"
                  ],
                  "properties": {
                    "reason": {
                      "type": "string"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": [
                "moderated"
              ],
              "properties": {
                "moderated": {
                  "type": "object",
                  "required": [
                    "outcome",
                    "reason"
                  ],
                  "properties": {
                    "outcome": {
                      "$ref": "#/definitions/ModerationOutcome"
                    },
                    "reason": {
                      "type": "string"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "Round": {
          "type": "object",
          "required": [
            "bounty_id",
            "contributor_count",
            "no_weight",
            "nomination",
            "number",
            "opens_at",
            "outcome",
            "rule",
            "total_weight",
            "voter_count",
            "yes_weight"
          ],
          "properties": {
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "closes_at": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Timestamp"
                },
                {
                  "type": "null"
                }
              ]
            },
            "contributor_count": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "finalized_at": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Timestamp"
                },
                {
                  "type": "null"
                }
              ]
            },
            "no_weight": {
              "$ref": "#/definitions/Uint128"
            },
            "nomination": {
              "$ref": "#/definitions/Nomination"
            },
            "number": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "opens_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "outcome": {
              "$ref": "#/definitions/RoundOutcome"
            },
            "rule": {
              "$ref": "#/definitions/RoundRule"
            },
            "total_weight": {
              "$ref": "#/definitions/Uint128"
            },
            "voter_count": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "yes_weight": {
              "$ref": "#/definitions/Uint128"
            }
          },
          "additionalProperties": false
        },
        "RoundOutcome": {
          "type": "string",
          "enum": [
            "pending",
            "paid",
            "declined",
            "no_majority",
            "tie",
            "no_votes"
          ]
        },
        "RoundRule": {
          "type": "string",
          "enum": [
            "sole_confirmation",
            "contribution_weighted_majority"
          ]
        },
        "Terms": {
          "type": "object",
          "required": [
            "acceptance_criteria",
            "config_version",
            "max_bounty_total",
            "max_contributors",
            "max_evidence_uri_bytes",
            "max_rationale_bytes",
            "max_reason_bytes",
            "max_rounds",
            "ratification_seconds",
            "summary",
            "title"
          ],
          "properties": {
            "acceptance_criteria": {
              "type": "string"
            },
            "config_version": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "content_digest": {
              "type": [
                "string",
                "null"
              ]
            },
            "content_uri": {
              "type": [
                "string",
                "null"
              ]
            },
            "max_bounty_total": {
              "$ref": "#/definitions/Uint128"
            },
            "max_contributors": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_evidence_uri_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_rationale_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_reason_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_rounds": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "ratification_seconds": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "summary": {
              "type": "string"
            },
            "title": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        }
      }
    },
    "claim": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Nullable_ClaimRecord",
      "anyOf": [
        {
          "$ref": "#/definitions/ClaimRecord"
        },
        {
          "type": "null"
        }
      ],
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "ClaimRecord": {
          "type": "object",
          "required": [
            "amount",
            "bounty_id",
            "claimed_at",
            "contributor"
          ],
          "properties": {
            "amount": {
              "$ref": "#/definitions/Uint128"
            },
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "claimed_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "contributor": {
              "$ref": "#/definitions/Addr"
            }
          },
          "additionalProperties": false
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        }
      }
    },
    "claims": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ClaimsResponse",
      "type": "object",
      "required": [
        "claims"
      ],
      "properties": {
        "claims": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ClaimRecord"
          }
        },
        "next_start_after": {
          "description": "Last scanned contributor index when another bounded page remains. Contributors without a completed claim can create holes in a page.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "ClaimRecord": {
          "type": "object",
          "required": [
            "amount",
            "bounty_id",
            "claimed_at",
            "contributor"
          ],
          "properties": {
            "amount": {
              "$ref": "#/definitions/Uint128"
            },
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "claimed_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "contributor": {
              "$ref": "#/definitions/Addr"
            }
          },
          "additionalProperties": false
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        }
      }
    },
    "config": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Config",
      "type": "object",
      "required": [
        "agent",
        "governor",
        "limits",
        "max_bounty_total",
        "max_contributors",
        "max_lifetime_seconds",
        "max_rounds",
        "min_contribution",
        "min_lifetime_seconds",
        "native_denom",
        "ratification_seconds",
        "registry",
        "version"
      ],
      "properties": {
        "agent": {
          "$ref": "#/definitions/Addr"
        },
        "governor": {
          "$ref": "#/definitions/Addr"
        },
        "limits": {
          "$ref": "#/definitions/Limits"
        },
        "max_bounty_total": {
          "$ref": "#/definitions/Uint128"
        },
        "max_contributors": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "max_lifetime_seconds": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_rounds": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "min_contribution": {
          "$ref": "#/definitions/Uint128"
        },
        "min_lifetime_seconds": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "native_denom": {
          "type": "string"
        },
        "ratification_seconds": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "registry": {
          "$ref": "#/definitions/Addr"
        },
        "version": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Limits": {
          "type": "object",
          "required": [
            "max_acceptance_criteria_bytes",
            "max_page_limit",
            "max_rationale_bytes",
            "max_reason_bytes",
            "max_summary_bytes",
            "max_title_bytes",
            "max_uri_bytes"
          ],
          "properties": {
            "max_acceptance_criteria_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_page_limit": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_rationale_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_reason_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_summary_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_title_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_uri_bytes": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "contribution": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ContributionView",
      "type": "object",
      "required": [
        "bounty_id",
        "contributor",
        "contributor_index",
        "current_amount"
      ],
      "properties": {
        "bounty_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "contributor": {
          "$ref": "#/definitions/Addr"
        },
        "contributor_index": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "current_amount": {
          "$ref": "#/definitions/Uint128"
        },
        "weight_at_round": {
          "anyOf": [
            {
              "$ref": "#/definitions/Uint128"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "contributions": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ContributionsResponse",
      "type": "object",
      "required": [
        "contributions"
      ],
      "properties": {
        "contributions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ContributionView"
          }
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "ContributionView": {
          "type": "object",
          "required": [
            "bounty_id",
            "contributor",
            "contributor_index",
            "current_amount"
          ],
          "properties": {
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "contributor": {
              "$ref": "#/definitions/Addr"
            },
            "contributor_index": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "current_amount": {
              "$ref": "#/definitions/Uint128"
            },
            "weight_at_round": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Uint128"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "error_catalog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ErrorCatalogResponse",
      "type": "object",
      "required": [
        "codes"
      ],
      "properties": {
        "codes": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ErrorCode"
          }
        }
      },
      "additionalProperties": false,
      "definitions": {
        "ErrorCode": {
          "type": "string",
          "enum": [
            "unauthorized",
            "unexpected_funds",
            "invalid_funds",
            "invalid_configuration",
            "invalid_metadata",
            "not_found",
            "invalid_state",
            "paused",
            "expired",
            "not_expired",
            "contribution_limit",
            "round_limit",
            "wrong_round",
            "not_contributor",
            "voting_closed",
            "ratification_open",
            "already_claimed",
            "not_refundable",
            "not_project_candidate",
            "already_graduated",
            "arithmetic"
          ]
        }
      }
    },
    "health": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "HealthResponse",
      "type": "object",
      "required": [
        "accounting",
        "actual_native_balance",
        "fully_backed",
        "liabilities"
      ],
      "properties": {
        "accounting": {
          "$ref": "#/definitions/Accounting"
        },
        "actual_native_balance": {
          "$ref": "#/definitions/Uint128"
        },
        "fully_backed": {
          "type": "boolean"
        },
        "liabilities": {
          "$ref": "#/definitions/Uint128"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Accounting": {
          "type": "object",
          "required": [
            "active_escrow",
            "lifetime_paid",
            "lifetime_received",
            "lifetime_refunded",
            "outstanding_refunds",
            "pending_payout_liabilities"
          ],
          "properties": {
            "active_escrow": {
              "$ref": "#/definitions/Uint128"
            },
            "lifetime_paid": {
              "$ref": "#/definitions/Uint128"
            },
            "lifetime_received": {
              "$ref": "#/definitions/Uint128"
            },
            "lifetime_refunded": {
              "$ref": "#/definitions/Uint128"
            },
            "outstanding_refunds": {
              "$ref": "#/definitions/Uint128"
            },
            "pending_payout_liabilities": {
              "$ref": "#/definitions/Uint128"
            }
          },
          "additionalProperties": false
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "history": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "HistoryResponse",
      "type": "object",
      "required": [
        "entries"
      ],
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HistoryEntry"
          }
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "HistoryAction": {
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "created",
                "contributed",
                "cancelled",
                "expired",
                "refunded",
                "graduated"
              ]
            },
            {
              "type": "object",
              "required": [
                "nominated"
              ],
              "properties": {
                "nominated": {
                  "type": "object",
                  "required": [
                    "round"
                  ],
                  "properties": {
                    "round": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": [
                "sole_confirmed"
              ],
              "properties": {
                "sole_confirmed": {
                  "type": "object",
                  "required": [
                    "round"
                  ],
                  "properties": {
                    "round": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": [
                "sole_declined"
              ],
              "properties": {
                "sole_declined": {
                  "type": "object",
                  "required": [
                    "round"
                  ],
                  "properties": {
                    "round": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": [
                "voted"
              ],
              "properties": {
                "voted": {
                  "type": "object",
                  "required": [
                    "round",
                    "vote"
                  ],
                  "properties": {
                    "round": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    },
                    "vote": {
                      "$ref": "#/definitions/PayoutVote"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": [
                "finalized"
              ],
              "properties": {
                "finalized": {
                  "type": "object",
                  "required": [
                    "outcome",
                    "round"
                  ],
                  "properties": {
                    "outcome": {
                      "$ref": "#/definitions/RoundOutcome"
                    },
                    "round": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": [
                "moderated"
              ],
              "properties": {
                "moderated": {
                  "type": "object",
                  "required": [
                    "outcome"
                  ],
                  "properties": {
                    "outcome": {
                      "$ref": "#/definitions/ModerationOutcome"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "HistoryEntry": {
          "type": "object",
          "required": [
            "action",
            "actor",
            "at",
            "bounty_id",
            "sequence"
          ],
          "properties": {
            "action": {
              "$ref": "#/definitions/HistoryAction"
            },
            "actor": {
              "$ref": "#/definitions/Addr"
            },
            "at": {
              "$ref": "#/definitions/Timestamp"
            },
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "sequence": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        "ModerationOutcome": {
          "type": "string",
          "enum": [
            "spam",
            "duplicate",
            "policy_violation"
          ]
        },
        "PayoutVote": {
          "type": "string",
          "enum": [
            "yes",
            "no"
          ]
        },
        "RoundOutcome": {
          "type": "string",
          "enum": [
            "pending",
            "paid",
            "declined",
            "no_majority",
            "tie",
            "no_votes"
          ]
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        }
      }
    },
    "pause": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PauseState",
      "type": "object",
      "required": [
        "paused"
      ],
      "properties": {
        "actor": {
          "anyOf": [
            {
              "$ref": "#/definitions/Addr"
            },
            {
              "type": "null"
            }
          ]
        },
        "changed_at": {
          "anyOf": [
            {
              "$ref": "#/definitions/Timestamp"
            },
            {
              "type": "null"
            }
          ]
        },
        "paused": {
          "type": "boolean"
        },
        "reason": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        }
      }
    },
    "receipt": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Nullable_VoteReceipt",
      "anyOf": [
        {
          "$ref": "#/definitions/VoteReceipt"
        },
        {
          "type": "null"
        }
      ],
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "PayoutVote": {
          "type": "string",
          "enum": [
            "yes",
            "no"
          ]
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        },
        "VoteReceipt": {
          "type": "object",
          "required": [
            "bounty_id",
            "cast_at",
            "revised_at",
            "revisions",
            "round",
            "vote",
            "voter",
            "voter_index",
            "weight"
          ],
          "properties": {
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "cast_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "rationale": {
              "type": [
                "string",
                "null"
              ]
            },
            "revised_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "revisions": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "round": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "vote": {
              "$ref": "#/definitions/PayoutVote"
            },
            "voter": {
              "$ref": "#/definitions/Addr"
            },
            "voter_index": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "weight": {
              "$ref": "#/definitions/Uint128"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "receipts": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ReceiptsResponse",
      "type": "object",
      "required": [
        "receipts"
      ],
      "properties": {
        "receipts": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/VoteReceipt"
          }
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "PayoutVote": {
          "type": "string",
          "enum": [
            "yes",
            "no"
          ]
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        },
        "VoteReceipt": {
          "type": "object",
          "required": [
            "bounty_id",
            "cast_at",
            "revised_at",
            "revisions",
            "round",
            "vote",
            "voter",
            "voter_index",
            "weight"
          ],
          "properties": {
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "cast_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "rationale": {
              "type": [
                "string",
                "null"
              ]
            },
            "revised_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "revisions": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "round": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "vote": {
              "$ref": "#/definitions/PayoutVote"
            },
            "voter": {
              "$ref": "#/definitions/Addr"
            },
            "voter_index": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "weight": {
              "$ref": "#/definitions/Uint128"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "round": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Round",
      "type": "object",
      "required": [
        "bounty_id",
        "contributor_count",
        "no_weight",
        "nomination",
        "number",
        "opens_at",
        "outcome",
        "rule",
        "total_weight",
        "voter_count",
        "yes_weight"
      ],
      "properties": {
        "bounty_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "closes_at": {
          "anyOf": [
            {
              "$ref": "#/definitions/Timestamp"
            },
            {
              "type": "null"
            }
          ]
        },
        "contributor_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "finalized_at": {
          "anyOf": [
            {
              "$ref": "#/definitions/Timestamp"
            },
            {
              "type": "null"
            }
          ]
        },
        "no_weight": {
          "$ref": "#/definitions/Uint128"
        },
        "nomination": {
          "$ref": "#/definitions/Nomination"
        },
        "number": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "opens_at": {
          "$ref": "#/definitions/Timestamp"
        },
        "outcome": {
          "$ref": "#/definitions/RoundOutcome"
        },
        "rule": {
          "$ref": "#/definitions/RoundRule"
        },
        "total_weight": {
          "$ref": "#/definitions/Uint128"
        },
        "voter_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "yes_weight": {
          "$ref": "#/definitions/Uint128"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Nomination": {
          "type": "object",
          "required": [
            "evidence_digest",
            "evidence_uri",
            "nominator",
            "rationale",
            "recipient"
          ],
          "properties": {
            "evidence_digest": {
              "type": "string"
            },
            "evidence_uri": {
              "type": "string"
            },
            "nominator": {
              "$ref": "#/definitions/Addr"
            },
            "rationale": {
              "type": "string"
            },
            "recipient": {
              "$ref": "#/definitions/Addr"
            }
          },
          "additionalProperties": false
        },
        "RoundOutcome": {
          "type": "string",
          "enum": [
            "pending",
            "paid",
            "declined",
            "no_majority",
            "tie",
            "no_votes"
          ]
        },
        "RoundRule": {
          "type": "string",
          "enum": [
            "sole_confirmation",
            "contribution_weighted_majority"
          ]
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        }
      }
    },
    "rounds": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RoundsResponse",
      "type": "object",
      "required": [
        "rounds"
      ],
      "properties": {
        "rounds": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Round"
          }
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Nomination": {
          "type": "object",
          "required": [
            "evidence_digest",
            "evidence_uri",
            "nominator",
            "rationale",
            "recipient"
          ],
          "properties": {
            "evidence_digest": {
              "type": "string"
            },
            "evidence_uri": {
              "type": "string"
            },
            "nominator": {
              "$ref": "#/definitions/Addr"
            },
            "rationale": {
              "type": "string"
            },
            "recipient": {
              "$ref": "#/definitions/Addr"
            }
          },
          "additionalProperties": false
        },
        "Round": {
          "type": "object",
          "required": [
            "bounty_id",
            "contributor_count",
            "no_weight",
            "nomination",
            "number",
            "opens_at",
            "outcome",
            "rule",
            "total_weight",
            "voter_count",
            "yes_weight"
          ],
          "properties": {
            "bounty_id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "closes_at": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Timestamp"
                },
                {
                  "type": "null"
                }
              ]
            },
            "contributor_count": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "finalized_at": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Timestamp"
                },
                {
                  "type": "null"
                }
              ]
            },
            "no_weight": {
              "$ref": "#/definitions/Uint128"
            },
            "nomination": {
              "$ref": "#/definitions/Nomination"
            },
            "number": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "opens_at": {
              "$ref": "#/definitions/Timestamp"
            },
            "outcome": {
              "$ref": "#/definitions/RoundOutcome"
            },
            "rule": {
              "$ref": "#/definitions/RoundRule"
            },
            "total_weight": {
              "$ref": "#/definitions/Uint128"
            },
            "voter_count": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "yes_weight": {
              "$ref": "#/definitions/Uint128"
            }
          },
          "additionalProperties": false
        },
        "RoundOutcome": {
          "type": "string",
          "enum": [
            "pending",
            "paid",
            "declined",
            "no_majority",
            "tie",
            "no_votes"
          ]
        },
        "RoundRule": {
          "type": "string",
          "enum": [
            "sole_confirmation",
            "contribution_weighted_majority"
          ]
        },
        "Timestamp": {
          "description": "A point in time in nanosecond precision.\n\nThis type can represent times from 1970-01-01T00:00:00Z to 2554-07-21T23:34:33Z.\n\n## Examples\n\n``` # use cosmwasm_std::Timestamp; let ts = Timestamp::from_nanos(1_000_000_202); assert_eq!(ts.nanos(), 1_000_000_202); assert_eq!(ts.seconds(), 1); assert_eq!(ts.subsec_nanos(), 202);\n\nlet ts = ts.plus_seconds(2); assert_eq!(ts.nanos(), 3_000_000_202); assert_eq!(ts.seconds(), 3); assert_eq!(ts.subsec_nanos(), 202); ```",
          "allOf": [
            {
              "$ref": "#/definitions/Uint64"
            }
          ]
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        },
        "Uint64": {
          "description": "A thin wrapper around u64 that is using strings for JSON encoding/decoding, such that the full u64 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u64` to get the value out:\n\n``` # use cosmwasm_std::Uint64; let a = Uint64::from(42u64); assert_eq!(a.u64(), 42);\n\nlet b = Uint64::from(70u32); assert_eq!(b.u64(), 70); ```",
          "type": "string"
        }
      }
    }
  }
}
